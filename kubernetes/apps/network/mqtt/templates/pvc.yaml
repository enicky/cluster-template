---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-mqtt
  namespace: mqtt
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 20M
