apiVersion: v1
kind: Service
metadata:
  name: unifi-service
  namespace: home
spec:
  ports:
  - name: http
    port: #{ bootstrap_unifi.port }#
    protocol: TCP
    targetPort: #{ bootstrap_unifi.port }#
