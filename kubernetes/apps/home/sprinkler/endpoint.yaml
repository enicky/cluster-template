apiVersion: v1
kind: Endpoints
metadata:
  name: sprinkler-service
  namespace: home
subsets:
- addresses:
  - ip: #{ bootstrap_sprinkler.host }#
  ports:
  - port: #{ bootstrap_sprinkler.port }#
    name: http
