# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: "Kubeconform"
on:
  pull_request:
    branches: ["main"]
    paths: ["kubernetes/**"]
env:
  KUBERNETES_DIR: ./kubernetes
jobs:
  kubeconform:
    name: Kubeconform
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Homebrew
        uses: Homebrew/actions/setup-homebrew@master
      - name: Setup Workflow Tools
        run: brew install argocd kubeconform kustomize
      - name: Install KSOPS
        run: |
          wget https://github.com/viaduct-ai/kustomize-sops/releases/latest/download/ksops_$(uname -s)_x86_64.tar.gz
          tar -xzf ksops_$(uname -s)_x86_64.tar.gz
          chmod +x ksops_$(uname -s)_x86_64
          sudo mv ksops_$(uname -s)_x86_64 /usr/local/bin/ksops
      - name: Check KSOPS version
        run: ksops --version
      - name: Run kubeconform
        shell: bash
        run: bash ./scripts/kubeconform.sh ${{ env.KUBERNETES_DIR }}
